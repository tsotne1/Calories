<% @food.each do |food| %>
  <div>
    <%= food.name %>
    <%= food.calories %>
  </div>

  <%= link_to "Add", food_meals_path(:food_id => food.id), class: "btn btn-sm btn-success", method: :post %>
<% end %>
